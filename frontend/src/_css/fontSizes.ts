export const fontSizes = {
    fs10: "clamp(0.6rem, 0.5vw, 0.625rem)",   // ~10px
    fs12: "clamp(0.7rem, 0.6vw, 0.75rem)",    // ~12px
    fs14: "clamp(0.8rem, 0.8vw, 0.875rem)",   // ~14px
    fs16: "clamp(0.9rem, 1vw, 1rem)",         // ~16px
    fs18: "clamp(1rem, 1.2vw, 1.125rem)",     // ~18px
    fs20: "clamp(1.1rem, 1.4vw, 1.25rem)",    // ~20px
    fs24: "clamp(1.3rem, 2vw, 1.5rem)",       // ~24px
    fs28: "clamp(1.5rem, 2.5vw, 1.75rem)",    // ~28px
    fs32: "clamp(1.6rem, 3vw, 2rem)",         // ~32px
    fs36: "clamp(1.8rem, 3.5vw, 2.25rem)",    // ~36px
    fs40: "clamp(2rem, 4vw, 2.5rem)",         // ~40px
    fs48: "clamp(2.5rem, 5vw, 3rem)",         // ~48px
    fs56: "clamp(3rem, 6vw, 3.5rem)",         // ~56px
    fs60: "clamp(3.2rem, 6.5vw, 3.75rem)",    // ~60px
};